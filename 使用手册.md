# 囚牛 (QiuNiu) 使用手册

> 版本：2026-05-27  
> 适用对象：系统管理员、员工用户、最终用户（客户）

---

## 目录

1. [系统概述](#系统概述)
2. [管理员指南](#管理员指南)
3. [员工用户指南](#员工用户指南)
4. [最终用户（客户）指南](#最终用户客户指南)
5. [常见问题](#常见问题)

---

## 系统概述

囚牛（QiuNiu）是一套**智能知识库问答系统**，包含三大使用角色：

| 角色 | 入口地址 | 主要操作 |
|------|----------|----------|
| 管理员 | `/login.jsp` | 管理知识库、提示词、配置角色 |
| 员工 | `/employee/employee-login.jsp` | 回答客户提问、管理个人头像 |
| 客户（最终用户） | `/knowledge-base.jsp` | 与 AI 对话、向本人提问 |

**核心流程：**

```
客户提问 → AI 基于知识库回答
    ↓（不满意）
客户点击「向本人提问」
    ↓
问题推送到员工门户
    ↓
员工回答 → 自动录入知识库 → 下次 AI 直接回答
```

---

## 管理员指南

### 登录

1. 访问 `http://localhost:8080/qiuniu/login.jsp`
2. 输入账号密码（默认 `admin / admin123`）
3. 首次登录建议修改密码

### 知识库管理

登录后，点击顶部导航栏 **「📖 知识库」** 进入知识库页面。

#### 手动录入知识点

1. 点击右上角 **「+ 新增」** 按钮
2. 填写表单：
   - **问题**：客户可能提问的关键词或完整问句
   - **答案**：标准回答内容
   - **分类**：用于筛选（如：开户、转账、理财、贷款）
   - **启用**：取消勾选可临时隐藏该知识点
3. 点击 **「保存」**

> 💡 **建议**：问题尽量简短、覆盖关键词，AI 检索时做模糊匹配。

#### 从文档导入知识点

1. 点击 **「📄 文档导入」**
2. 选择 Word（`.docx`）、PDF（`.pdf`）或纯文本（`.txt`）文件
3. 系统自动解析文档内容，按段落拆分为多个知识点
4. 导入完成后，在列表中逐个**审核并启用**

支持格式：
- Word：`.docx`（2007+ 格式，`.doc` 不支持）
- PDF：可选（扫描版 PDF 无法提取文字，需先 OCR）
- 纯文本：`.txt`（UTF-8 编码）

#### 测试检索效果

在知识库页面的**底部输入框**输入测试问题，观察 AI 回答是否命中知识库：

- ✅ 命中：回答下方显示「参考来源：知识点 N」
- ❌ 未命中：AI 基于自身知识回答，建议补充对应知识点

#### 编辑 / 删除知识点

- **编辑**：点击列表中「编辑」按钮
- **删除**：点击「删除」按钮（物理删除，不可恢复）
- **禁用**：编辑时将「启用」取消勾选（软删除，可恢复）

### 虚拟角色（AI 人设）管理

每个虚拟角色对应一套独立的**系统提示词（人设）**和关联的知识库。

目前通过**数据库直接操作**管理角色：

```sql
-- 查看所有角色
SELECT id, name, title, is_active FROM virtual_characters;

-- 新增角色
INSERT INTO virtual_characters
  (name, title, description, system_prompt, categories, confidence_threshold, sort_order, is_active)
VALUES
  ('smart_cs', '智能客服', '回答客户常见问题',
   '你是某银行的智能客服助手，语气专业、简洁。如无法回答，引导客户联系人工客服。',
   '开户,转账,理财', 0.2, 1, 1);

-- 关联员工（该角色收到的问题会推送给此员工）
UPDATE virtual_characters SET employee_id = <员工ID> WHERE id = <角色ID>;
```

**字段说明：**

| 字段 | 说明 |
|------|------|
| `name` | 角色英文标识（用于 URL 路由） |
| `title` | 角色显示名称 |
| `system_prompt` | AI 人设提示词 |
| `categories` | 该角色负责的知识分类（逗号分隔） |
| `confidence_threshold` | 低于此分数时触发「向本人提问」 |
| `employee_id` | 关联的员工 ID（回答推送目标） |
| `sort_order` | 排序权重（越小越靠前） |
| `is_active` | 是否启用（0=禁用，1=启用） |

### 员工管理

目前通过**数据库直接操作**录入员工：

```sql
-- 注册新员工（密码用 bcrypt 加密）
INSERT INTO employees (employee_no, name, password_hash, email, dept, position, is_active)
VALUES
  ('EMP001', '唐佳艺', '$2b$12$...（bcrypt哈希）', 'tangjy@example.com', '客服部', '客服专员', 1);

-- 生成 bcrypt 哈希（Python）：
--   import bcrypt; bcrypt.hashpw('123456'.encode(), bcrypt.gensalt()).decode()
```

> ⚠️ **注意**：员工密码必须经过 BCrypt 加密，不可明文存储。

### 提示词管理

点击顶部 **「控制台」** 进入提示词管理页面。

- **新建**：右上角「+ 新建提示词」
- **编辑 / 删除**：操作列对应按钮
- **搜索**：顶部搜索框输入关键词
- **分类筛选**：下拉框选择分类

---

## 员工用户指南

### 登录员工门户

1. 访问 `http://localhost:8080/qiuniu/employee/employee-login.jsp`
2. 输入**工号**（如 `EMP001`）和**密码**
3. 首次登录默认密码为 `123456`，登录后系统提示修改

### 回答客户提问

登录后自动进入「待回答问题」列表：

1. 每个问题显示：**提问内容、关联角色、提交时间**
2. 点击问题右侧的 **「回答」** 按钮
3. 在弹窗中填写答案
4. 点击 **「提交」**
5. ✅ 提交后，系统自动将 Q&A 录入知识库，该问题从待回答列表消失

> 💡 **建议**：回答尽量标准、完整，这样 AI 以后可以直接复用你的回答。

### 上传个人头像

1. 点击页面右上角**头像区域**
2. 选择本地图片（支持 JPG/PNG，建议正方形）
3. 上传成功后，客户在知识库聊天页面看到的就是你的头像

### 修改密码

点击右上角姓名 → 「修改密码」，输入原密码和新密码。

---

## 最终用户（客户）指南

### 与 AI 对话

1. 访问 `http://localhost:8080/qiuniu/knowledge-base.jsp`
2. 在底部输入框输入问题（如「怎么开卡？」）
3. AI 基于知识库内容回答，并附上参考来源
4. 如果回答有帮助，点击**「👍」**；没有帮助，点击**「👎」**

### 向本人提问

如果对 AI 回答不满意：

1. 点击 AI 回复下方的 **「没有我满意的答案，我要向本人提问」**
2. 问题自动推送到对应员工的「待回答问题」列表
3. 员工回答后，答案自动录入知识库
4. 下次再问同样的问题，AI 就能直接给出标准答案了

---

## 常见问题

### 管理员常见问题

**Q：知识库导入文档后，AI 回答还是不准？**  
A：检查知识点是否**启用**（列表中绿色开关），以及问题表述是否覆盖了客户可能的提问方式。建议多录入几条相似问法。

**Q：新增的角色在聊天页面看不到？**  
A：检查 `virtual_characters` 表的 `is_active` 字段是否为 `1`。

**Q：员工登录提示「工号或密码错误」？**  
A：确认 `employees` 表的 `is_active` 是否为 `1`，且 `password_hash` 是有效的 BCrypt 哈希（以 `$2b$` 或 `$2a$` 开头）。

### 员工常见问题

**Q：登录后看不到「待回答问题」？**  
A：检查 `virtual_characters` 表的 `employee_id` 是否正确关联了你的员工 ID。执行以下 SQL 验证：

```sql
SELECT v.name, v.employee_id, e.name
FROM virtual_characters v
LEFT JOIN employees e ON v.employee_id = e.id
WHERE e.id = <你的员工ID>;
```

**Q：上传头像后客户那边没变化？**  
A：尝试让客户**刷新页面**（头像有浏览器缓存）。如果还是不行，检查 `employees.avatar_uploaded_path` 字段是否有值。

### 最终用户常见问题

**Q：点「向本人提问」没反应？**  
A：检查 Flask RAG 服务是否正常运行（`http://localhost:5001/status`）。如果 Flask 服务停止，该功能无法使用。

**Q：AI 回答很慢？**  
A：回答速度取决于大模型 API 的响应时间。可更换更快的模型，或检查网络连接。

---

## 附录：快捷命令

```bash
# 启动 Tomcat
cd C:\apache-tomcat-9.0.96\bin && startup.bat

# 启动 Flask RAG 服务
cd D:\qiuniu20260506\bank_kb && python app.py

# 查看 Tomcat 日志
tail -f C:\apache-tomcat-9.0.96\logs\catalina.out

# 查看 Flask 日志
# 直接在 Flask 运行窗口查看

# 重启所有服务（Windows）
D:\qiuniu20260506\qiuniu-src\deploy.bat
```

---

*本手册基于 2026-05-27 版本编写，功能以实际部署版本为准。*
