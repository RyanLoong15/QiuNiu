# QiuNiu Flask Backend Dependencies
# 适用于 Linux ARM64 (麒麟) 环境
# pip install -r requirements.txt

# === Web Framework ===
flask>=3.0.0
Flask-CORS>=4.0.0
Werkzeug>=3.0.0

# === Database ===
mysql-connector-python>=8.0.0

# === Document Parsing ===
python-docx>=1.1.0          # .docx 解析（含表格/页眉页脚/文本框）
python-pptx>=0.6.23         # .pptx 解析
pypdf>=4.0.0                # PDF 文本提取
PyMuPDF>=1.23.0             # PDF 图片/文本/布局提取（含 fitz 模块）
openpyxl>=3.1.0             # .xlsx/.xlsm 解析（合并单元格/富文本）

# === OCR（扫描版 PDF，可选——纯图片 PDF 会降级提示）===
rapidocr-onnxruntime>=1.3.0  # 纯 Python OCR，无 Tesseract 依赖

# === Full-Text Search / RAG ===
jieba>=0.42.1               # 中文分词
rank-bm25>=0.2.0            # BM25 检索算法
numpy>=1.24.0

# === Auth ===
bcrypt>=4.0.0               # 密码哈希
PyJWT>=2.8.0                # JWT 令牌

# === HTTP Client ===
requests>=2.31.0

# === Vector Search（可选——仅混合检索模式需要）===
# pymilvus>=2.4.0
